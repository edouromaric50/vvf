import 'package:flutter/material.dart';

const primaryColor = Color(0xff22A2F2);

class AppColor {
  static const primary = primaryColor;
  static var white = Colors.white;

  static const catgColor = Colors.blue;
  static const projeColor = Colors.green;
  static const caisseColor = Colors.amber;
  static const settingColor = Color.fromARGB(255, 89, 253, 206);
}
